const action = {
  type: 'LOGIN'
}
// Define an action creator here:
{/* An action creator is simply a JavaScript function that returns an action */}
{/* In other words, action creators[f(n)] / create objects[return event;] / that represent action events */}
function actionCreator() { 
  return action;
}
